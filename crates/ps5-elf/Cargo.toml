[package]
name = "ps5-elf"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ELF64 binary format parser (zero-copy)"

[dependencies]
ps5-format = { path = "../ps5-format" }

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
